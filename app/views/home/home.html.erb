<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <style>
        /* Internal CSS */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            padding-top: 70px; /* Adjust for fixed navbar */
        }

        /* Navbar Styling */
        .navbar {
            background-color: #333;
            overflow: hidden;
            padding: 10px 20px;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            padding: 10px 15px;
        }

        .navbar a:hover {
            background-color: #575757;
            border-radius: 5px;
        }

        /* Main Content */
        .container {
            text-align: center;
            padding: 50px;
        }

        h1 {
            color: #333;
        }

        p {
            font-size: 18px;
            color: #555;
        }

        /* Footer */
        .footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px;
            position: fixed;
            width: 100%;
            bottom: 0;
        }
    </style>
</head>
<body>

    <!-- Bootstrap Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Todo List</a>
            <div class="ml-auto">
                <% if user_signed_in? %>
                    <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
                <% else %>
                    <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-primary mr-2" %>
                    <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-success" %>
                <% end %>
            </div>
        </div>
    </nav>

    <!-- Authentication Section -->
    <div class="container">
        <% if user_signed_in? %>
            <h2 class="welcome-text">Welcome, <%= current_user.email %>!</h2>
            <p>
                <%= link_to "Edit Profile", edit_user_registration_path, class: 'btn btn-info' %>
            </p>
        <% else %>
            <h2 class="welcome-text">Welcome to the Todo List App!</h2>
            <p>Please <%= link_to "log in", new_user_session_path %> or <%= link_to "sign up", new_user_registration_path %> to continue.</p>
        <% end %>
    </div>

  

    <!-- Footer -->
    <div class="footer">
        &copy; 2025 My Website. All Rights Reserved.
    </div>

</body>
</html>
